# 🔐 Simple Auth - Supabase Static Site

Минимальная рабочая система регистрации и входа на **статическом сайте** с использованием **Supabase Auth**.

## ✨ Что реализовано

- ✅ **Чистый HTML/CSS/JS** (нет React, Vue, Vite, сборки)
- ✅ **Supabase Auth** через ESM CDN (без npm в браузере)
- ✅ **Статический деплой** на Vercel (**без бэкенда, без Express**)
- ✅ **Никакого бэкенда** (никакого server.js, никаких API endpoints)
- ✅ **Нет таблицы profiles** (данные в встроенном Supabase Auth)
- ✅ **Нет RLS политик** (не требуются для Auth)
- ✅ **Пароли не хранятся** (управляются только Supabase)

## 📁 Структура проекта

```
/test
├── config.js                  # Supabase ключи (SUPABASE_URL, SUPABASE_ANON_KEY)
├── app.js                     # Основная логика UI + обработчики форм
├── index.html                 # Экраны: регистрация, вход, статус
├── styles.css                 # Стили UI
├── src/
│   ├── auth.js               # Функции: signUp, signIn, signOut, getUser, onAuthStateChange
│   └── supabaseClient.js     # Инициализация Supabase клиента (ждёт загрузки CDN)
├── package.json              # npm зависимости (для npm start)
├── server.js                 # Локальный сервер (http://localhost:4173)
├── vercel.json              # Конфиг статического деплоя на Vercel
└── SETUP.md                 # Эта инструкция
```

## 🚀 Быстрый старт

### 1️⃣ Ключи Supabase уже установлены

В файле [config.js](config.js) уже заполнены:
```javascript
export const SUPABASE_URL = 'https://cboyfzjntbfnmlslgccm.supabase.co'
export const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
```

### 2️⃣ Запустить локально

```bash
npm install
npm start
```

Откройте **http://localhost:4173** в браузере.

### 3️⃣ Тестирование

#### Форма регистрации:
1. Email: `test@example.com`
2. Пароль: `securePassword123` (минимум 6 символов)
3. Телефон: (опционально) `+79991234567`
4. Нажмите **"Зарегистрироваться"**

#### Проверка в Supabase:
1. Откройте https://supabase.com → ваш проект
2. **Authentication** → **Users**
3. Должен появиться новый пользователь `test@example.com`

#### Вход:
1. На странице нажмите **"Войти"**
2. Email: `test@example.com`
3. Пароль: `securePassword123`
4. Нажмите **"Войти"**
5. На странице должен показаться ваш email + кнопка **"Выйти"**

#### Выход:
1. Нажмите **"Выйти"**
2. Вернётесь на форму регистрации

## 🔧 Как работает

### Функции аутентификации

Все в файле [src/auth.js](src/auth.js):

```javascript
// Регистрация с опциональным телефоном
signUp(email, password, phone = null)
// → supabase.auth.signUp({ email, password, options: { data: { phone } }})

// Вход
signIn(email, password)
// → supabase.auth.signInWithPassword({ email, password })

// Выход
signOut()
// → supabase.auth.signOut()

// Получить текущего пользователя
getUser()
// → data.user из supabase.auth.getUser()

// Подписаться на изменения
onAuthStateChange(callback)
// → supabase.auth.onAuthStateChange() с обновлением UI
```

### UI логика

[app.js](app.js) обрабатывает:
- Переключение между экранами (регистрация, вход, статус)
- Отправку форм (с валидацией)
- Показ сообщений об ошибках/успехе
- Обновление показываемого email после входа

## 🌐 Деплой на Vercel

### 1. Подготовка кода
```bash
git add .
git commit -m "Supabase auth - static site"
git push origin main
```

### 2. На Vercel.com:
1. Откройте https://vercel.com
2. **Add New** → **Project**
3. Импортировать GitHub репозиторий
4. Vercel обнаружит [vercel.json](vercel.json) автоматически
5. Нажать **Deploy**

### 3. После развёртывания:
- Сайт работает как **статический** (HTML/CSS/JS, без сборки)
- URL: например `https://your-project.vercel.app`
- Суть аутентификация работает сразу (ключи в config.js)

## 🔒 Безопасность

| Компонент | Где? | Безопасно? |
|-----------|------|-----------|
| **SUPABASE_URL** | config.js | ✅ (публично видна всегда) |
| **SUPABASE_ANON_KEY** | config.js | ✅ (для фронтенда, ограничена RLS) |
| **Пароли** | Supabase Auth только | ✅ (не видны, зашифрованы) |
| **Сессия (JWT)** | localStorage | ✅ (автоматически обновляется) |
| **Email пользователя** | Supabase + UI | ✅ (видно после входа) |

> Примечание: **Anon Key** безопасна для фронтенда, потому что Supabase ограничивает операции на уровне **RLS политик**. В нашем случае мы не используем RLS, т.к. нет необходимости защищать доступ к каким-то таблицам (для Auth это не требуется).

## ❓ FAQ

### Вопрос: Где хранятся пароли?
**Ответ:** Только в **Supabase Auth**. Не сохраняются в БД приложения, не логируются, не отправляются на бэкенд.

### Вопрос: Нужна ли таблица `profiles`?
**Ответ:** **НЕТ**. Все необходимые данные (email, phone) хранятся в встроенном Supabase Auth:
```javascript
user.email           // из auth.users
user.user_metadata   // custom данные (phone и др.)
```

### Вопрос: Как добавить дополнительные поля (ФИО, аватар)?
**Ответ:** Используйте `options.data` в `signUp`:
```javascript
signUp('user@example.com', 'password', {
  data: {
    full_name: 'John Doe',
    avatar_url: 'https://...',
    phone: '+79991234567'
  }
})
```
Затем доступ: `user.user_metadata.full_name`

### Вопрос: "Cannot read property 'createClient' of undefined"
**Ответ:** CDN Supabase не загрузился:
1. Проверьте интернет
2. Перезагрузите страницу (Ctrl+Shift+R)
3. Проверьте консоль (F12) на ошибки сети

### Вопрос: Пользователь не появляется в Auth → Users
**Ответ:** Возможные причины:
- **Email confirmation включен** → пользователь появится после подтверждения
- **Ошибка в форме** → смотрите консоль браузера (F12 → Console)
- **Неправильные ключи** → проверьте SUPABASE_URL и SUPABASE_ANON_KEY в config.js

### Вопрос: Нужен ли server.js?
**Ответ:** Только для локальной разработки (`npm start`). На Vercel используется встроенный статический сервер.

## 🎯 Что НЕ включено (и почему)

| Компонент | Почему? |
|-----------|---------|
| **React/Vue/Svelte** | Не требуются для простой auth |
| **Vite/Webpack/Parcel** | Статический сайт, сборка не нужна |
| **Express/Node.js API** | Vercel статический хостинг |
| **PostgreSQL таблица profiles** | Данные в Supabase Auth Auth |
| **RLS политики** | Не требуются для Auth |
| **npm dependencies** | Supabase через CDN, нет npm в браузере |
| **Логирование паролей** | Пароли только в Supabase |

## 🔍 Файлы и их роль

| Файл | Роль |
|------|------|
| [config.js](config.js) | 🔑 Ключи Supabase |
| [src/auth.js](src/auth.js) | 🔐 Функции аутентификации |
| [src/supabaseClient.js](src/supabaseClient.js) | 🌐 Инициализация клиента |
| [app.js](app.js) | 🎮 UI логика (обработчики, экраны) |
| [index.html](index.html) | 🎨 HTML структура |
| [styles.css](styles.css) | 🎨 Стили |
| [server.js](server.js) | 🖥️ Локальный сервер |
| [package.json](package.json) | 📦 npm конфиг |
| [vercel.json](vercel.json) | ☁️ Vercel конфиг |

## 📝 Примеры использования

### Проверить, авторизован ли пользователь
```javascript
import { getUser } from './src/auth.js'

const user = await getUser()
if (user) {
  console.log(`Привет, ${user.email}!`)
} else {
  console.log('Не авторизован')
}
```

### Слушать изменения статуса
```javascript
import { onAuthStateChange } from './src/auth.js'

onAuthStateChange((event, session, user) => {
  console.log(`Событие: ${event}`, user?.email)
})
```

### Регистрация с метаданными
```javascript
import { signUp } from './src/auth.js'

await signUp('user@example.com', 'password123', {
  full_name: 'John Doe',
  birth_date: '1990-01-01'
})
```

## 🚀 Следующие шаги (опционально)

Если позднее понадобятся дополнительные функции:

### Вариант 1: Суpabase Database (хранить доп. данные)
```sql
CREATE TABLE public.profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  full_name TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own profile"
ON profiles
FOR SELECT
TO authenticated
USING (auth.uid() = id);
```

### Вариант 2: Supabase Edge Functions (серверная логика)
- Обработка платежей, отправка email и т.д.
- Развёртывается на Vercel

### Вариант 3: Supabase Storage (загрузка файлов)
- Аватары, документы и т.д.
- Встроено в Supabase Dashboard

## 📞 Контакты / Поддержка

- Документация Supabase Auth: https://supabase.com/docs/guides/auth
- Vercel Static Sites: https://vercel.com/docs/frameworks/static-site-generation

---

**Проект создан:** 5 февраля 2026  
**Версия:** 1.0.0  
**Статус:** ✅ Production Ready
