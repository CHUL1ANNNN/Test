PR Plan for feature/simple-auth-ui

- Обновить локальный main и переключиться на ветку feature/simple-auth-ui (ветка уже создана в удалённом репозитории).
- Добавить страницы/компоненты: /register и /login с простыми формами (email/телефон/пароль).
- Реализовать минимальные серверные эндпоинты (mock): POST /api/register и POST /api/login — возвращают 200 и простой JSON.
- Подключить базовую маршрутизацию и минимальные стили для форм.
- Добавить инструкции по запуску и короткие шаги для тестирования в локальной среде.

Границы (что делаем в ЭТОМ PR):
- Делаем: базовый UI регистрации и входа + минимальные ответы сервера (файловое хранилище).
- НЕ делаем: безопасность, подтверждение email/SMS, токены/сессии, хэширование паролей, капчу.

