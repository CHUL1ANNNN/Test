# Auth Interface Agent (GitHub)

Простой “учебный” проект: **Регистрация → сохранение на сервере → Вход**.  
Без сложной защиты, подтверждений email/SMS и продвинутых механизмов безопасности — базовый end-to-end поток.

---

## ▶️ Запуск проекта (frontend + backend)

```bash
node server.js
```

Сервер поднимает:
- UI: `http://localhost:4173`
- API: `POST /register`, `POST /login`

> Отдельный фронтенд-сервер не нужен: `server.js` обслуживает и статику, и API.

---

## 🧪 Как проверить сценарий register → login → welcome

1. Откройте `http://localhost:4173`.
2. На экране **Регистрация** введите новый email/телефон/пароль и нажмите **Зарегистрироваться**.
   - Ожидаемо: сообщение об успехе и переход на экран входа.
3. Вернитесь на регистрацию и попробуйте зарегистрироваться с тем же email.
   - Ожидаемо: ошибка `Этот email уже занят.`
4. На экране **Вход** введите корректные данные и нажмите **Войти**.
   - Ожидаемо: переход на экран **Добро пожаловать**.
5. Нажмите **Выйти** и снова выполните вход с неверным паролем.
   - Ожидаемо: ошибка `Неверные данные для входа.`

---

## ✅ Правила (упрощённые)

### Регистрация
- Все поля обязательны
- `email` должен быть уникальным
- `phone` должен быть уникальным
- Пароль минимум 6 символов

Ошибки:
- Пустые поля
- Email уже существует
- Телефон уже существует

### Вход
- Вводится `login` (email или phone) + пароль
- Сервер ищет пользователя по email **или** phone
- Сравнивает пароль

Ошибки:
- Пользователь не найден
- Пароль неверный

---

## 📦 Scope текущей реализации

Включено:
- Базовый UI регистрации/входа/welcome
- Минимальная клиентская валидация
- Файловое хранение пользователей (`users.json`)
- Простые API-ответы для отображения ошибок/успеха

Не включено:
- Хэширование паролей
- Токены/сессии
- Подтверждение email/SMS
- CAPTCHA и защита от брутфорса

---


## ▲ Vercel (Stage 1/2)

Параметры деплоя зафиксированы в `docs/vercel-deploy.txt` и `vercel.json`:
- Framework: `Other`
- Root Directory: `.`
- Build Command: `npm run build`
- Output Directory: `.`

Переменные окружения вынесены в `docs/vercel-env.txt` (на текущем этапе обязательных переменных нет).

## 📄 Лицензия
MIT (или другая на выбор).
