{
  "name": "auth-interface-agent",
  "private": true,
  "version": "1.0.0",
  "description": "Simple auth training app",
  "scripts": {
    "build": "node -e \"console.log('No build step required: static assets are in repository root.')\"",
    "start": "node server.js"
  }
}
